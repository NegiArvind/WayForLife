<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minWidth="300sp"
    android:minHeight="180sp">


    <android.support.design.widget.TextInputLayout
        android:id="@+id/forgetEmailTextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30sp"
        android:layout_marginStart="24sp"
        android:layout_marginEnd="24sp">

        <EditText
            android:id="@+id/forgetEmailEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:inputType="textEmailAddress"
            android:hint="@string/email_hint"/>

    </android.support.design.widget.TextInputLayout>

    <Button
        android:id="@+id/forgetPasswordButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/forgetEmailTextInputLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30sp"
        android:layout_marginBottom="10sp"
        android:background="@drawable/button_gradient_round_shape"
        android:padding="18sp"
        android:textColor="@color/md_white_1000"
        android:textStyle="bold"
        android:text="@string/password_reset_link"/>

</RelativeLayout>